{
  "name": "rocketlink",
  "version": "1.1.0",
  "description": "Proximity voice chat client for RocketLeague",
  "main": "src/main.js",
  "scripts": {
    "start": "electron --inspect .",
    "package": "electron-packager . RocketLink --platform=win32 --arch=x64 --out=dist/ --icon=icon.ico --ignore=\"(build-installer\\.js|\\.idea)\" --prune=true --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Yannis Matezki",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "electron": "26.1.0",
    "electron-packager": "^17.1.2",
    "electron-winstaller": "^5.1.0"
  },
  "dependencies": {
    "electron-store": "^8.1.0"
  }
}
